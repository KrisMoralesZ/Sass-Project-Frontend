# Backend origin (NestJS API). No trailing slash, no /api/v1 suffix —
# the app appends the versioned path itself (see src/lib/env.ts).
# Only VITE_-prefixed vars are exposed to the browser.
VITE_API_URL=http://localhost:3000
